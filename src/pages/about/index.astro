---
import SitePage from '../../layouts/SitePage.astro';
import CompanyPage from '../../components/interior/CompanyPage.astro';
import TestimonialsBand from '../../components/interior/TestimonialsBand.astro';
import TeamBand from '../../components/interior/TeamBand.astro';
import page from '../../data/pages/about.json';
const h = page.hero;
---
<SitePage title={page.title} description={page.seoDescription}>
  <CompanyPage
    eyebrow={h.eyebrow}
    title={h.title}
    intro={h.intro}
    image={h.image}
    imagePosition={h.imagePosition}
    backgroundColor={h.backgroundColor}
    overlayColor={h.overlayColor}
    overlayOpacity={h.overlayOpacity}
    headingColor={h.headingColor}
    introColor={h.introColor}
    accentColor={h.accentColor}
    headingFont={h.headingFont}
    sections={page.sections}
  />
  <TeamBand />
  <TestimonialsBand page="about" />
</SitePage>
